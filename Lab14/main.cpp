#include <iostream>
#include "Functions.h"

int main() {
    //while (selection());

    auto tree = buildBalancedTree(15);
    createBalancedSearchTree(tree);
    tree = insertToTree(tree, 2993);
    tree = insertToTree(tree, 22);
    tree = insertToTree(), -10);
    printTree(tree, 0);

    return 0;
}
